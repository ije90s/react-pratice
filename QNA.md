# 질문과 답변

> 학습 중에 나온 질문과 답변을 주제별로 쌓는 문서. 단계별 진행 기록은 `LEARNING.md`, 여기에는 "왜 그런가 / 언제 쓰는가" 같은 개념 질문만 정리한다.
> 항목 형식: `## [날짜] 제목` → 질문 → 답변 → 관련 단계

---

## [2026-09-19] `<Navigate>`는 언제 쓰고, `replace`는 뒤로가기 방지인가?

**질문:**
1. `<Navigate>`는 JSX 안에서 리다이렉트할 때 쓰는 것인가?
2. `replace`는 뒤로가기를 막는 옵션인가?

**답변:**

### 1) `<Navigate>` vs `useNavigate()` — "렌더링 결과로 결정하느냐, 이벤트 뒤에 하느냐"

| 상황 | 도구 | 이 프로젝트의 예 |
|---|---|---|
| 렌더링 중에 "이 화면은 못 보여줌, 저리 가" | `<Navigate>` (JSX로 반환) | `AppLayout`의 `if (!token) return <Navigate to="/login" replace />` |
| 이벤트나 비동기 작업이 끝난 뒤 이동 | `useNavigate()`의 `navigate()` | `LoginPage`의 로그인 성공 후 `navigate("/challenges")` |

- **렌더링 중에 `navigate()`를 직접 호출하면 안 된다.** 렌더링은 화면을 계산하는 순수한 과정이어야 하는데 이동은 부수 효과이기 때문이다. `<Navigate>`는 내부적으로 렌더링이 끝난 뒤 이펙트에서 이동을 실행해 이 문제를 피한다.
- 반대로 클릭 핸들러나 `await` 뒤에는 JSX를 반환할 수 없으니 `navigate()`를 쓴다.
- `useEffect` 안에서 `navigate()`를 부르는 것도 가능하지만, "조건이 맞으면 이동"이라는 선언적인 경우에는 `<Navigate>`가 더 간결하다.

### 2) `replace` — 뒤로가기를 막는 게 아니라 "히스토리에 항목을 추가하지 않고 현재 항목을 덮어쓴다"

```
replace 없음 (push):  [/login] → [/mypage] → [/login]      ← /mypage 항목이 남음
                       뒤로가기 → /mypage → 가드가 또 튕겨냄 → 루프

replace 있음:         [/login] → [/login]                   ← /mypage 항목이 대체됨
                       뒤로가기 → 그 이전 화면으로
```

- 뒤로가기 버튼 자체는 그대로 동작한다. 달라지는 건 **되돌아갈 곳에 보호 화면 항목이 남아 있느냐**이다.
- 보호 화면에 접근했다가 튕겨진 기록은 사용자가 의도한 이동이 아니므로 히스토리에 남길 필요가 없다. 그래서 가드, 로그인 후 이동, 잘못된 주소 정리 같은 **자동 리다이렉트에는 보통 `replace`를 쓴다.**
- 사용자가 직접 링크를 눌러 이동하는 일반 화면 전환에는 쓰지 않는다(그러면 뒤로가기로 이전 화면에 갈 수 없다).
- 히스토리는 스택이 아니라 "배열 + 현재 위치" 구조라서, `push`는 현재 위치 뒤에 항목을 추가하고 `replace`는 현재 위치의 항목을 교체한다.

### 실험으로 확인한 값 (17단계)

토큰 없이 `/mypage`로 들어갔을 때 히스토리 길이(시작 2 기준):

| | 리다이렉트 후 길이 | 뒤로가기 1회 후 URL |
|---|---|---|
| `replace` 없음 | 5 | `/login` |
| `replace` 있음 | 3 | `/login` |

뒤로가기 후 URL은 둘 다 `/login`이라 URL만으로는 차이가 안 보인다(가드가 보호 화면 복귀를 다시 튕겨내기 때문). 차이는 **히스토리에 쌓이는 항목 수**에서 드러난다. `replace` 없는 `Navigate`가 항목을 두 번 쌓는 것은 개발 모드 `StrictMode`가 이펙트를 두 번 실행하기 때문이다.

### 이 코드베이스에서 생각해 볼 곳

`LoginPage`의 `navigate("/challenges")`는 `replace` 없이 push한다. 로그인 성공 뒤 뒤로가기를 누르면 이미 로그인한 상태로 로그인 화면이 다시 보인다. 로그인 화면은 "지나온 임시 화면"이라 `navigate("/challenges", { replace: true })`가 더 자연스럽다(17단계 "남겨둔 것" ②와 같은 뿌리의 문제).

**관련 단계:** `LEARNING.md` 17단계 (공용 헤더 + 인증 가드)

---

## [2026-09-20] 401 공통 처리는 어디에 두는 게 좋은가? (`useFetch` vs `apiFetch` 콜백)

**질문:**
1. 401 공통 처리를 하려면 백엔드도 수정해야 하는가?
2. `useFetch`에서 처리하는 방식(1번)의 문제는 무엇이고, `apiFetch`에 콜백을 등록하는 방식(2번)은 "로그인 정보가 전역변수로 들어가서" 비추인가?
3. 지금 하면 결국 두 번 작업하는가? 처리 방식만 놓고 보면 무엇이 베스트인가?

**답변:**

### 1) 백엔드 수정은 필요 없다 — 이미 401을 준다

무효 토큰으로 `/user/me`, `/feed/...`를 호출해 직접 확인했다.

| 항목 | 백엔드 동작 |
|---|---|
| 무효 / 만료 / 없음 | 모두 `401 Unauthorized` (`ignoreExpiration: false`) |
| 토큰 수명 | `expiresIn: '1y'` (1년) |
| 리프레시 토큰 | 없음 |

401 공통 처리는 "서버가 401이면 로그아웃하고 `/login`으로 보낸다"는 **프론트만의 일**이다. 백엔드가 필요해지는 건 토큰 재발급(리프레시)을 도입할 때인데, 수명이 1년이라 실익이 적고 학습 범위 밖이다. 지금 구조에서는 401 = "다시 로그인"으로 충분하다. 401을 실제로 만나는 경우는 DB 초기화·`JWT_SECRET` 변경·계정 삭제·1년 경과 정도다.

### 2) 두 방식의 정확한 비교 (처음 설명을 정정)

- **1번 (`useFetch`에서 처리)** — 진짜 약점은 "로그인 401을 제외해야 한다"가 아니다(로그인은 `apiFetch`를 직접 부르므로 영향 없음). **`useFetch`는 GET 전용이라 뮤테이션(POST/PATCH/DELETE)이 처리되지 않는다**는 점이다. 새 호출 지점마다 처리를 붙여야 하고, 깜빡하면 그 화면만 `Unauthorized`가 뜬다.
- **2번 (콜백 등록)** — 전역에 들어가는 것은 토큰이 아니라 **"401이면 실행할 함수"**다. 토큰은 그대로 `AuthContext` state에 있다. 진짜 약점은 등록 시점 문제(`StrictMode`의 등록/해제 두 번), 눈에 안 보이는 결합, 콜백이 옛 `token`을 물고 있는 문제다.

| | 1번 | 2번 |
|---|---|---|
| 뮤테이션 커버 | 안 됨 | 됨 |
| 수정 범위 | `useFetch` (+ 나중에 뮤테이션마다) | `apiFetch` + `AuthProvider` |
| 흐름의 가시성 | 높음 | 낮음(숨은 결합) |

### 3) 결론: 2번이 처리 방식으로도, 작업량으로도 낫다

**책임 분리가 자연스럽다.** `apiFetch`(전송 계층)는 "서버가 401이라 했다"는 사실만 알리고, `AuthProvider`(인증 계층)가 그때 뭘 할지 결정한다. 1번은 401 정책이 모든 호출자에 흩어져 "잊기 쉬운 규칙"이 된다. 정책이 한 곳에 있으면 바꿀 때도 한 곳이다(예: "세션 만료" 안내 추가). axios 인터셉터, TanStack Query의 전역 에러 핸들러가 같은 모양이다.

1번은 지금 하면 Phase 4에서 뮤테이션 때문에 다시 손봐야 해서 부분적으로 두 번 작업이지만, 2번은 `apiFetch` 안에서 끝나 **뮤테이션 패턴과 무관하게** 한 번에 끝난다. (처음에 "Phase 4 이후에 정하는 게 낫다"고 한 것은 1번을 택할 때만 성립하는 논거였다.)

### 2번의 약점과 완화

| 약점 | 완화 |
|---|---|
| 숨은 결합 | 등록 지점을 `AuthProvider` 한 곳으로 제한하고 `client.ts`에 이유를 주석으로 |
| `StrictMode` 등록/해제 두 번 | `useEffect`의 cleanup에서 해제 |
| 콜백이 옛 `token`을 물고 있음(stale closure) | 의존성에 `token`을 넣어 재등록 |
| 동시 요청 여러 개가 401 | `logout()`이 여러 번 불려도 결과가 같게(멱등) |
| 로그인 실패도 401 | `token`을 보낸 요청일 때만 콜백 호출 |

**관련 단계:** `LEARNING.md` 18단계 "남겨둔 것", `SCREEN_PLAN.md` 보완 과제 "401 공통 처리"
